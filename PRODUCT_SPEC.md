# NodeSpeed — 产品期望规格

> 自建的「链路质量画像」面板：像 speed.cloudflare.com 那样出上下行曲线、加载延迟、抖动、
> bufferbloat 和场景化评级（游戏/视频/RTC），但测量对端是**你自己的 VPS 节点**，而不是 CF 边缘。

状态：设计阶段（尚未开工）。最后更新 2026-07-18。

---

## 1. 动机 / 要解决的痛点

- 多台远端 VPS，测网络质量每次手动开 iperf3、敲命令，麻烦；而且只能看速度，细节测不出来。
- LibreSpeed 界面丑、信息少。
- als（wikihost lookingglass）偏传统，数据不够专业。
- 最喜欢 speed.cloudflare.com：上下行速度曲线、不同速度下的延迟响应、按场景（游戏/视频/RTC）
  给链路综合评级。**唯一遗憾**：CF 只能测「我 ↔ CF 节点」，测不了「我 ↔ 我自己的服务器」。

**目标**：把 CF 那套体验搬过来，对端换成自己的节点，并补上 CF 没有的功能（节点一览、点击选择、地图链路、历史趋势）。

---

## 2. 核心决策（已锁定）

| 决策点 | 结论 |
|---|---|
| 测量视角 | **浏览器 → 节点**（client 视角，同 CF）。测量必须浏览器直连节点，绝不经中心转发。 |
| 测量引擎 | 复用 CF 开源的 `@cloudflare/speedtest`（MIT），把 `downloadApiUrl`/`uploadApiUrl` 指向自己节点。fork 掉结果回传 CF 的那段。 |
| 评级算法 | 复用 AIM（Aggregated Internet Measurement，开放标准、算法开源）。6 指标加权出 Bad→Great。 |
| 节点注册 | **手动**。节点清单在中心配置里维护。节点**不回连、不上报**（明确不做探针/自动注册）。 |
| 丢包 | **v1 不做**。6 个 AIM 指标先做 5 个（下行/上行/空载延迟/加载延迟/抖动+bufferbloat），丢包按 0 或标注"未测"。部署脚本留 `--enable-packetloss` 开关，将来按需给单个节点加 coturn。 |
| 鉴权 | **用户↔面板 这层不做，交给外层网关**（反代/SSO 等）。中心只做 浏览器↔节点 的短时令牌签发，节点无状态验签（详见 §5）。防止节点被外人滥用带宽。 |
| 节点 TLS | 多模式:`http` / `ip 自签 https` / `域名 https(ACME)` / `填本机证书路径`。一键脚本带 ACME 申请模块（复用 acme.sh）。 |
| 部署 | 中心 = 单 Docker 容器；节点 = 单 Go 静态二进制 + systemd，一键交互脚本装一次长期常驻。 |

---

## 3. 系统架构

```
┌─────────────────────────────────────┐        浏览器直连每个节点测量
│  中心看板（单 Docker 容器）           │       （client → node，不经中心）
│  - 登录（用户 ↔ 面板 鉴权）           │
│  - 节点表（手动配置：名称/子域名/地区/secret）│
│  - 令牌签发（用各节点 secret 离线签）  │◀──────┐ 打开面板
│  - 前端面板：一览表 + 连通性状态       │       │
│  - 世界地图 + 链路弧线                 │   你的浏览器
│  - 历史趋势（SQLite）                 │       │
└─────────────────────────────────────┘       │ 测量/ack（带令牌）
                                               ▼
              ┌──────────────┐  ┌──────────────┐  ┌─────┐
              │ 节点 agent    │  │ 节点 agent    │  │ ... │ 10+
              │ Go 静态二进制 │  │              │  │     │
              │ TLS(自签/ACME)│  │              │  │     │
              │ /__ack        │  │              │  │     │
              │ /__down /__up │  │              │  │     │
              │ +Server-Timing│  │              │  │     │
              │ 令牌验签+限流 │  │              │  │     │
              │ systemd 常驻  │  │              │  │     │
              └──────────────┘  └──────────────┘  └─────┘
              （被动服务，不回连中心）
```

---

## 4. 组件职责

### 4.1 中心看板（Docker 单容器）
- **不含登录**：用户↔面板的鉴权交给外层网关（反代 basic-auth / SSO / Cloudflare Access 等）。中心假定"能连到我 = 已被网关放行"。
- **节点表**：手动配置，每条记录 `{名称, 子域名/URL, 地区, 经纬度, secret[, 是否启用丢包]}`。
- **令牌签发**：用对应节点的 secret 离线签短时令牌（不需要连节点）。
- **前端面板**：
  - 打开即展示**节点一览表 + 连通性状态**（见 §6）。
  - 点选节点 → 跑完整测速 → CF 同款上下行曲线 + 加载/空载延迟 + 抖动 + AIM 场景评级。
  - **世界地图**：画「你 → 节点」链路弧线。坐标来自节点配置，比 CF 更准（CF 只能画到它自己机房）。
  - 支持多节点并排对比。
- **历史趋势**：SQLite 存历次结果，看链路随时间变化。

### 4.2 节点 agent（Go 静态二进制 + systemd）
- 单文件、systemd 常驻、装一次长期服务。
- **TLS 多模式**（浏览器直连需 HTTPS，除非中心也走 HTTP 内网调试）：
  - `http`：明文（仅本地/内网调试，或中心也是 HTTP）。
  - `ip 自签 https`：给裸 IP 自签证书（浏览器需手动信任）。
  - `域名 https(ACME)`：agent 内置或调 acme.sh 自动签/续。
  - `本机证书路径`：直接指向已有 `fullchain.pem`+`privkey.pem`（测试机现状:`/root/cert/andhk.sealee.dpdns.org/`）。
- **端点**（均带 `Server-Timing` 头，CORS 放行面板域名）：
  - `GET /__ack?token=…` → 极小响应，用于探活 + 空载 RTT + 鉴权预检。
  - `GET /__down?token=…&bytes=N` → 发 N 字节。
  - `POST /__up?token=…` → 收 POST 丢弃。
- **令牌验签**：用本机 secret 验 HMAC + 查过期，失败 403。
- **限流**（纵深防御，v1 可选）：单 IP 并发数、单令牌总字节配额。
- **不回连中心、不上报任何数据。**

### 4.3 一键交互脚本（在节点上跑）
- 交互式询问：`子域名 / 节点名 / 地区`（及可选 `--enable-packetloss`）。
- **TLS 模式选择**：http / ip 自签 / 域名 ACME / 填本机证书路径。选 ACME 时内置**申请模块**（复用 acme.sh 签发 + 装续期）。
- 生成 secret 并**打印出来**（你手动填进中心配置）。
- 装 systemd 服务，配好 TLS。
- **不**注册、**不**上报。

---

## 5. 鉴权设计（防滥用）

**用户 ↔ 面板 这层不在本系统内**——交给外层网关（反代 basic-auth / SSO / Cloudflare Access 等）。中心假定"能连到它 = 已被网关放行"。
本系统只管 **浏览器 ↔ 节点**（短时令牌），目的是防止节点端点被外人扫到滥用带宽。

```
1. 部署时：脚本生成 shared secret → 打印 → 你填进中心节点配置。仅此一次。
2. 测速/探活时：
   面板 → 中心用该节点 secret 签短时令牌（HMAC-SHA256，claims: {节点ID, exp≈2min, iat}）
        → 令牌发给浏览器
   浏览器 → 直连节点 /__down?token=xxx&bytes=…
          → 节点用本机 secret 验签 + 查过期 → 通过才发数据，否则 403
```

要点：
- **节点与中心零实时连接**：中心只需"知道每个节点 secret"即可离线签；节点只需用本机 secret 验签。预共享密钥间接建立信任。
- 令牌走 **query 参数**（不走 header）：和 CF 引擎的 `downloadApiUrl` 拼参方式天然契合，且免 CORS 预检。令牌短时、单节点作用域，进日志无所谓。
- 外人扫到端点也没有效令牌 → 403，带宽锁死在自己人。

---

## 6. 连通性一览表（打开面板即见）

- 面板加载时，前端对**每个已配置节点**发一次 ack 握手：中心签令牌 → 浏览器请求节点 `/__ack?token=…`。
- ack 同时完成三件事：**探活**（在线/离线）、**量空载 RTT**、**验鉴权**（令牌链路通不通）。
- 表格状态列示例：🟢 在线（RTT xx ms） / 🔴 不可达 / 🟡 鉴权失败。
- 用户看完状态再决定点哪个节点跑完整测速。

---

## 7. 必须接受的现实约束

- **每个节点需要自己的 DNS 名**（如 `hk1.speed.你的域名.com`）：因为浏览器直连 + HTTPS，
  且测量不能经中心转发（否则测的不是到节点的真实链路）。成本 = 每节点配一条 A 记录，agent 自己签证书。

---

## 8. 分期建议

- **一期**：节点 agent（`/__ack`/`/__down`/`/__up` + Server-Timing + 令牌验签）、一键脚本、
  中心（登录 + 手动节点表 + 令牌签发 + 复用 CF 引擎的前端 + 连通性一览表）。跑通「点选节点 → 出曲线 + AIM 评级」。
- **二期**：世界地图 + 链路弧线、历史趋势（SQLite）、多节点并排对比。
- **三期（按需）**：coturn 丢包、节点侧限流配额。

---

## 9. 关键外部参考

- CF 测量引擎（可复用）：https://github.com/cloudflare/speedtest
- AIM 评级算法/标准：https://developers.cloudflare.com/speed/aim/
- CF 测速原理博客：https://blog.cloudflare.com/how-does-cloudflares-speed-test-really-work/

---

## 10. 功能清单

完整、权威的功能清单（按页面 block 逐块拆解 + 我们的新增）见独立文件 **[FEATURES.md](FEATURES.md)**。
那份是复刻蓝图 + 验收 checklist 的唯一真源，本节不再重复以免漂移。
